#ifndef UTIL_H
#define UTIL_H

#include "main.h"
#include <string>
#include <sstream>
 
class Util{
	public:
		Util();
		double toDouble(string str);
		int toInt(string str);
	protected:
		
	private:
};

#endif
